{
  "name": "homebridge-xstate",
  "version": "1.0.0",
  "description": "XState Statemachines for Homebridge",
  "main": "dist/xstate-platform.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "rimraf ./dist && tsc",
    "prepublishOnly": "npm run build",
    "postpublish": "npm run clean",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint -c .eslintrc.json --ignore-pattern=\"node_modules\" --ext js .",
    "lint-fix": "eslint -c .eslintrc.json --ignore-pattern=\"node_modules\" --ext js --fix ."
  },
  "author": "aceslick911",
  "engines": {
    "homebridge": ">=1.0.0"
  },
  "license": "ISC",
  "files": [
    "LICENSE",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/aceslick911/homebridge-xstate"
  },
  "bugs": {
    "url": "https://github.com/aceslick911/homebridge-xstate/issues"
  },
  "devDependencies": {
    "@types/node": "10.17.19",
    "typescript": "^3.8.3",
    "rimraf": "^3.0.2",
    "homebridge": "^1.0.4",
    "eslint": "^7.0.0",
    "eslint-plugin-no-autofix": "^1.1.2"
  }
}